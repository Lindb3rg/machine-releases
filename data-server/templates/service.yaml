apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-server.fullname" . }}
  labels:
    {{- include "data-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-server.selectorLabels" . | nindent 4 }}
